# Merge queue control

Inert file used to verify that `ci-gate`, `plugins-gate` and `docs-gate`
all report on a pull request before the merge queue is enabled.

Safe to delete once merge-queue testing is finished.
